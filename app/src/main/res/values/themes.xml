<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ObsBankAlerts" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores de marca que son constantes -->
        <item name="colorPrimary">@color/brand_orange</item>
        <item name="colorPrimaryVariant">@color/brand_orange_dark</item>
        <item name="colorSecondary">@color/brand_teal</item>

        <!-- Fondos que ahora leen de `colors.xml` o `values-night` dependiendo del sistema -->
        <item name="android:windowBackground">@color/bg_dark_main</item>
        <item name="android:colorBackground">@color/bg_dark_main</item>
        <item name="colorSurface">@color/bg_card_surface</item>
        
        <!-- Textos por defecto -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="colorOnSurface">@color/text_primary</item>
    </style>
</resources>